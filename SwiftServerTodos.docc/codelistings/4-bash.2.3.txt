Select "Prometheus" from the data source drop-down.

In the Builder mode, try one of the system metrics emitted by
`SystemMetricsMonitor`:

  `process_cpu_seconds_total`

The Metric combobox lists all the reported metrics, for example
`http_requests_total` repoted by Vapor.
